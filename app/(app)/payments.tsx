import { PlaceholderScreen } from "@/shared/ui/PlaceholderScreen";

export default function PaymentsRoute() {
	return (
		<PlaceholderScreen
			title="Payments"
			subtitle="Track who paid, who is due, and monthly collections. Reminders can hook in here later."
		/>
	);
}
