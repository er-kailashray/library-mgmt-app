/** AsyncStorage keys (shared across features). */
export const STORAGE_KEYS = {
	onboardingComplete: "@librum/onboarding_complete",
	authToken: "@librum/auth_token",
	authUser: "@librum/auth_user",
} as const;
